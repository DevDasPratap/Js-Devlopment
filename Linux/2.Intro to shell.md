create shell file, REPL


Note:
For any linux command you can actually check the usage and diffrent input flags it expects by running the command followed by --help

Relative and absolute paths

Absolute paths: When you give the absolute path of a file or folder that means you will give the whole path of that file or folder.
Relative paths: whereas in the relative path you do jumps