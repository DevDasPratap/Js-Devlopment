Cookies: these are mainly conserned towords privacy
http is stateless protocol and a lot of times user session is required

how cookies work:
cookies are unique idendifer string, these are set by server through http header, so as soon as as cookies is stored,
it is sent along with subsequest http request to the same server.
this allow to know who is contacting it and hence serve the content accordingly

set cookies headers:
when a server want to set a cokkie it include "set-cookie":"value" in the http response

this value is stored in the cookie file of browser
