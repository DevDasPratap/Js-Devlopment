import { greet } from "../app"

test('greet returns the correct tgreeting', ()=>{
    // AAA
    // - Arrange
    // - Act
    // - Assert
    const expect = 'Hello, World!'
    const actual = greet

})