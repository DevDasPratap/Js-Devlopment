/**
 * Index: the position of an element in the array is known as its index
 * index start with 0
 * index end with length - 1
 */

// How to create array
// using literal
const salad = ['tomato', 'banana', 'apple', 'guava', 'pinaple']

// using constructure function
function Car(model) {
    this.model = model
}

const bmwCar = new Car('BMW X')

// instance
const anotherSalad = new Array('tomato', 'banana', 'apple', 'guava', 'pinaple')
const three = new Array(3)
console.log(three)