const arr = [1, 2, 3, 4, -10]

const maxSumContiguousSubarray = (arr) =>{
    for(let i=0; i<arr.length; i++){
        console.log(arr[i])
    }
}
const res = maxSumContiguousSubarray(arr)
console.log(res)