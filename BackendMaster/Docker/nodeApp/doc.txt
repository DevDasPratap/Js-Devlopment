inside docker if you want to run command so goto bash like: latest bash
Encryption, Hashing, Encoding - What's Really The Difference?
1. Why encoding is not great for security?
2. Why Hashing is only one way, but is a go-to method for storing passwords?
3. Why Encryption is designed to be Decrypted eventually?