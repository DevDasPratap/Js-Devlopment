component of v8 engine
 - parser
 - interpeter
 - compiler
internal of v8 engine:
v8 engine help to run js code in a machine


parser: 
usercase: it is to takes js code tokenise it and creates AST

tokenise:


interpeter: it name as ignition interpeter it covert AST to bytecode.
usercase:

how to check bytecode?
create any .js file
if run node filename it will execute return final output
but node provide to see you .js file into bytecode
run: node --print-bytecode filename.js


compiler: turbofan(optimization compiler)


all 3 happenning in run time and we call JIT
 - parser
 - interpeter
 - compiler

two types of compilation:
HOT and JIT
HOT: c++ (before running code it compile it and then run it)
JIT: JavaScript (you start running your code, interpreter takes your code convert into bytecode and then you do compilation top of it, so during the run time doing compilation and running together thats we call as a JIT)

v8 engine provide memory aspect:
 - access to the call stack
 - access to the heap

v8 engine also handle/provides garbage collection capabilities
and algorithm used orinoco. it perfectly also during code execution
orinoco: 
how work orinoco: